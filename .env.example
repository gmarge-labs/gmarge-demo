# Copy to .env and fill in. .env is git-ignored and must never be committed.
# Used only by offline read-out generation -- never by the Streamlit app.

# Required by `python -m gmarge.analyst` (not needed for --dry-run).
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-haiku-4-5.
GMARGE_MODEL=
